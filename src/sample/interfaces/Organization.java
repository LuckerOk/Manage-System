package sample.interfaces;

public interface Organization
{
    void addOrganization(sample.objects.Organization organization);
    void deleteOrganization(sample.objects.Organization organization);
}
